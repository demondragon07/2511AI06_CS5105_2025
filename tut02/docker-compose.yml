services:
  streamlit_app:
    build: .
    container_name: faculty_allocation_app
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
